<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="board">
	
	<!-- 프로필 작성 -->
	<insert id="insertProfile" parameterType="p">
		insert into profile values(p_seq.nextval,#{pWriter},#{pGender},#{pAge},#{pLocal},#{pSmoke},#{pPet},#{pCleaning},#{pPattern})
	</insert>
	
	<!-- 프로필 조회 -->
	<select id="selectProfile" parameterType="String" resultType="p">
		select 
			p_no as pNo,
			p_writer as pWriter,
			p_gender as pGender,
			p_age as pAge,
			p_local as pLocal,
			p_smoke as pSmoke,
			p_pet as pPet,
			p_cleaning as pCleaning,
			p_pattern as pPattern
		from profile where P_writer=#{memberId}
	</select>
	
	<!-- 프로필 수정 -->
	<update id="updateProfile" parameterType="p">
		update profile set 
			p_writer=#{pWriter},
			p_gender=#{pGender},
			p_age=#{pAge},
			p_local=#{pLocal},
			p_smoke=#{pSmoke},
			p_pet=#{pPet},
			p_cleaning=#{pCleaning},
			p_pattern=#{pPattern}
		where p_writer=#{pWriter}
	</update>
	
	<!-- 프로필 삭제 -->
	<delete id="deleteProfile" parameterType="String">
		delete from profile where p_writer=#{pWriter}
	</delete>
	
		<!-- 회원조회  -->
	<select id="selectOneMember" parameterType="int" resultType="m">
		select 
			member_no as memberNo,
			member_id as memberId,
			member_pw as memberPw,
			member_name as memberName,
			phone,
			email,
			enroll_date as enrollDate,
			member_filepath as filepath,
			member_level as memberLevel,
			gender,
			age,
			profile_status as profileStatus	
 		from member where member_no=#{memberNo}
	</select>
	
	<!-- 회원조회  -->
	<select id="selectMember" parameterType="String" resultType="m">
		select 
			member_no as memberNo,
			member_id as memberId,
			member_pw as memberPw,
			member_name as memberName,
			phone,
			email,
			enroll_date as enrollDate,
			member_filepath as filepath,
			member_level as memberLevel,
			gender,
			age,
			profile_status as profileStatus	
 		from member where member_Id=#{memberId}
	</select>
	
	<!-- 게시글 목록 조외 -->
	<select id="bdList" resultType="b">
		select 
			BOARD_NO as boardNo,
			BOARD_WRITER as boardWriter,
			BOARD_TITLE as boardTitle,
			BOARD_CONTENT as boardContent,
			READ_COUNT as readCount,
			REG_DATE as regDate,
			FILEPATH,
			PROFILE_OPTION as profileOption,
			STATUS
		from board			
	</select>
	
	<!-- 게시글 목록 -->
	<select id="boardList" resultType="b">
		select 
			BOARD_NO as boardNo,
			BOARD_WRITER as boardWriter,
			BOARD_TITLE as boardTitle,
			BOARD_CONTENT as boardContent,
			READ_COUNT as readCount,
			REG_DATE as regDate,
			FILEPATH,
			PROFILE_OPTION as profileOption,
			STATUS
		from board where board_no=#{boardNo}			
	</select>
	
	<!-- 게시글 작성 -->
	<insert id="insertBoard" parameterType="b">
		insert into board values(board_seq.nextval,#{boardWriter},#{boardTitle},#{boardContent},
		0,to_char(sysdate,'yyyy-mm-dd HH:MM'),#{filePath},(select p_gender||p_age||p_local||p_smoke||p_pet||p_cleaning||p_pattern from profile where p_writer=#{boardWriter}),1)
	</insert>
	
	<!-- 게시글 조회수 -->
	<update id="updateCount" parameterType="b">
		update board set
			read_count = read_count+1
		where board_no=#{boardNo}
	</update>

	<!-- 게시글 목록 -->
	<select id="selectBoardList" parameterType="int" resultType="b">
		select * from board where board_no=#{boardNo}		
	</select>

	<!-- 게시물 수정 -->
	<update id="boardUpdate" parameterType="b">
		update board set
			filepath=#{filePath},
			board_title=#{boardTitle},
			board_content=#{boardContent}
		where board_no=#{boardNo}
	</update>
	
	<!-- 게시물 삭제 -->
	<delete id="boardDelete" parameterType="b">
		delete from board where board_no=#{boardNo}
	</delete>
	
	<!-- 회원 파일경로 -->
	<select id="selectBoardMember" parameterType="b" resultType="string">
		select member_filepath from member where member_id=(select board_writer from board where board_no=#{boardNo})
	</select>
</mapper>





